{
  "name": "personal-finance-tracker",
  "version": "1.0.0",
  "description": "Complete MERN Personal Finance Tracker Application",
  "scripts": {
    "install-all": "npm install && npm install --prefix backend && npm install --prefix frontend",
    "start": "concurrently \"npm run prod --prefix backend\" \"npm start --prefix frontend\"",
    "dev": "concurrently \"npm run dev --prefix backend\" \"npm start --prefix frontend\""
  },
  "devDependencies": {
    "@types/chai": "^5.2.2",
    "@types/mocha": "^10.0.10",
    "@types/sinon": "^17.0.4",
    "chai": "^5.2.1",
    "concurrently": "^7.0.0",
    "mocha": "^11.0.1",
    "sinon": "^21.0.0"
  },
  "author": "Personal Finance Tracker Team",
  "license": "ISC",
  "dependencies": {
    "express": "^5.1.0"
  }
}
